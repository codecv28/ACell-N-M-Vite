import { createContext } from "react";

export const view_Gallery_Context = createContext(false)
export const view_individual_Flipbook_Context = createContext(false)
export const pdf_Context = createContext(null)